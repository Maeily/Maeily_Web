import React from "react";
import "./LoginFormWrapper.scss";

function LoginFormWrapper({ children }) {
    return <div className="LoginForm_Wrapper">{children}</div>;
}

export default LoginFormWrapper;
